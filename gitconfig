[user]
  name = Rubem Nakamura
  email = rubem.nakamura@gmail.com
[alias]
  st = status
  co = checkout
  ci = commit
  br = branch
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
[color]
  branch = auto
  diff = auto
  status = auto

[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green

[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold

[color "status"]
  added = yellow
  changed = green
  untracked = cyan
[core]
  editor = vim
  excludesfile = /Users/nakamura/.gitignore
[github]
  user = rubemz
[web]
  browser = ch
[browser "ff"]
  cmd = open -a Firefox.app
[push]
  default = current
[browser "ch"]
  cmd = open -a \"/Applications/Google Chrome.app\"
